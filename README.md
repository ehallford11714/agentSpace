# Agentic AI App Stack

This repository will contain a complete AI application stack with a web UI, agentic RAG backend, and video generation worker.

## Quick start

Bring up the stack with:

```bash
docker compose up --build
```

Documentation and component-specific details will expand as each task is implemented.
